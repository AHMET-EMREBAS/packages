# yaml-language-server: $schema=../../../schemas/model.schema.json
name: Permission
properties:
  permission:
    type: string
    minLength: 3
    maxLength: 30
    inputType: text

unique:
  - permission

required:
  - permission
