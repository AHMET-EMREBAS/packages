# yaml-language-server: $schema=../../../schemas/model.schema.json
name: Quantity
properties:
  quantity:
    type: number
    minimum: 0
    inputType: number

relations:
  sku:
    type: owner
    target: Sku

  store:
    type: owner
    target: Store

required:
  - sku
