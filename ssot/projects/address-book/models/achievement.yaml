# yaml-language-server: $schema=../../../schemas/model.schema.json
name: Achievement
properties:
  description:
    type: string
    inputType: textarea

relations:
  user:
    target: User
    type: owner

required:
  - description
