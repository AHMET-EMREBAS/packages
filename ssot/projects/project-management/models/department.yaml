# yaml-language-server: $schema=../../../schemas/model.schema.json
name: Department
properties:
  name:
    type: string
    minLength: 3
    maxLength: 30
    inputType: text

unique:
  - name

required:
  - name
