# yaml-language-server: $schema=../schemas/model.schema.json

properties:
  name:
    type: string
    inputType: text

  description:
    type: string
    maxLength: 300
    inputType: text

relations:
  category:
    type: subs
    target: Category

# Add required properties
required:
  - name

# Add unique properties
unique:
  - name
