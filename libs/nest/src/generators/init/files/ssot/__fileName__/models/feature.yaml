# yaml-language-server: $schema=../schemas/model.schema.json
name: Feature
properties:
  feature:
    type: string
    maxLength: 400
    inputType: textarea

unique:
  - feature

required:
  - feature
