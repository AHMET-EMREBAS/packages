# yaml-language-server: $schema=../../../schemas/model.schema.json
name: SocialMedia

properties:
  link:
    type: string
    maxLength: 30
    inputType: text

relations:
  user:
    type: owner
    target: User

unique:
  - link

required:
  - link
